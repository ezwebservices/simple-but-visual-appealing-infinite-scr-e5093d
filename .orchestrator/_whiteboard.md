# Iteration Whiteboard

**Change request:** the logo and the landing page still have old characters

**Subtasks planned:** 3

1. **Designer**: Redraw brand/logo.svg (also served as /brand/logo.svg favicon + nav mark in landing/index.html at lines 53 and 283, and dist/brand/logo.svg) so the characters shown in the logo match the current in-app roster defined in src/components/characters/*.tsx and the palettes in .orchestrator/update_landing_chars.py: Sunny=fox (warm orange), Rosie=bunny (soft pink), Milo=frog (green), Pip=panda (grayscale), Rex=robot (cyan), Bloo=bear (blue). Consult .orchestrator/numpals_brand_bible.md and numpals_art_notes.md for brand constraints. Keep the logo compact enough to read at 40px (nav) and favicon sizes. Export/update both brand/logo.svg and dist/brand/logo.svg so the live build is consistent. Do NOT change the wordmark or overall layout unless the old characters are structurally blocking the redesign — this is a character swap, not a rebrand.
2. **Engineer**: Update landing/index.html so every character reference matches the current in-app roster. Steps: (1) Run .orchestrator/update_landing_chars.py to regenerate the 6 character SVGs embedded in landing/index.html (the script is already wired to mirror src/components/characters/CharacterRig.tsx palettes and decorations). (2) Grep landing/index.html for any hardcoded character names, emojis, or counts that reference the OLD roster (e.g. '6 animal characters' copy is fine, but check for stale names like old 'zebra'/'owl'/'bug' mentions in hero copy, meta description (line 9), og:description (line 17), schema.org description (line 37), and the '#characters' section header near line 289). Update them to match the new roster: Sunny the fox, Rosie the bunny, Milo the frog, Pip the panda, Rex the robot, Bloo the bear. (3) Verify the regenerated SVGs render correctly by opening landing/index.html in a browser — confirm all 6 characters display with correct palettes and no rendering artifacts. (4) Do NOT touch the logo (Designer owns that). Do NOT modify src/components/characters/* — those are the source of truth. Commit regenerated landing/index.html and any copy fixes together.
3. **QA**: Verify the logo and landing page match the in-app characters end-to-end. Checks: (1) Open landing/index.html in a browser — confirm the favicon (tab icon), nav logo (top-left at line 283), and all 6 character illustrations in the #characters section visually match the corresponding CharacterDisplay renderings in src/components/characters/*.tsx (Sunny=fox/orange, Rosie=bunny/pink, Milo=frog/green, Pip=panda/grayscale, Rex=robot/cyan, Bloo=bear/blue). (2) Grep landing/index.html for any surviving references to the PREVIOUS roster (zebra, owl, bug, old fox, old names) in copy, alt text, meta tags, or schema.org JSON-LD — flag any found. (3) Build the app (npm run build or equivalent) and confirm dist/brand/logo.svg has been updated to match brand/logo.svg. (4) Load the app itself and confirm the in-app character rendering is unchanged (regression check — we must not have modified src/components/characters/*). (5) Report pass/fail with screenshots of the landing hero, #characters section, and the favicon in the browser tab.

---

