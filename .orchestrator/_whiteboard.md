# Iteration Whiteboard

**Change request:** this app seems to only work device, A child account is different across devices , a account should be accessble same progress across devices?

**Subtasks planned:** 3

1. **Architect**: Define the cross-device sync contract for child profiles and progress. Current state: ChildProfile model exists in amplify/data/resource.ts:13-25 with owner-auth, and src/hooks/useChildProfiles.ts has cloudLoading + merge logic (commit 85959ab). Gaps to resolve: (a) src/hooks/useProgress.ts is localStorage-only — decide whether to fold aggregate stats (totalCorrect, bestStreak, currentStreak, longestStreak, sessionCount) into the existing ChildProfile model as new JSON fields OR create a separate Progress model; recommend the simpler option that avoids breaking the 1 paying user's existing cloud data. (b) Audit useChildProfiles.ts updateProfile / addActivity / updateMastery paths — confirm every mutation debounce-writes to DynamoDB, not just on explicit save. (c) Specify the conflict-resolution rule when the same child is modified on two devices (recommend last-write-wins using an updatedAt timestamp added to ChildProfile, with per-field merge for conceptProgress so mastery gains on device A aren't overwritten by a stale read on device B). (d) Specify backward-compat: any new schema fields must be optional so existing records keep loading. Deliverable: write a short spec to .orchestrator/_sync_contract.md (≤200 lines) with: data model diff, write-through triggers, conflict rules, migration-safety checklist, and explicit list of files the Engineer will touch. Do NOT modify code.
2. **Engineer**: Implement cross-device sync per .orchestrator/_sync_contract.md (written by Architect in the previous subtask — read it first). Core work: (1) extend Amplify schema in amplify/data/resource.ts to persist whatever the contract specifies (likely additional JSON fields on ChildProfile + an updatedAt timestamp) — keep every new field optional so existing records for the 1 paying subscriber continue to load without migration. (2) Wire useProgress.ts to read/write through the cloud via the same pattern as useChildProfiles.ts (initial cloud fetch gated by cloudLoading, debounced write-through on every mutation, localStorage as offline cache). (3) Ensure every mutation in useChildProfiles.ts (addActivity, updateMastery, updateProfile, recentActivity updates) debounce-writes to DynamoDB — not only on explicit save. (4) Implement the conflict-resolution rule from the contract (most-recent updatedAt wins at the profile level; per-concept merge inside conceptProgress so mastery never regresses). (5) Run `npm run build` locally and fix any type errors. Do NOT alter auth flow, subscription logic, or the Stripe/webhook lambdas. Do NOT commit/push — QA runs next.
3. **QA**: Verify cross-device sync end-to-end. Test matrix: (1) Sign in on Device A (or browser profile A), create child 'TestA', play a lesson to raise mastery on 'counting to 5', log out. Sign in on Device B with the same account — confirm TestA appears with the SAME mastery, activity history, and aggregate progress (totalCorrect, bestStreak). (2) With both devices signed in, update the child's avatar on A — confirm it appears on B after a refresh/re-sign-in. (3) Offline test: disconnect Device A, play a session, reconnect — confirm writes flush to cloud and land on Device B. (4) Conflict test: modify same child on A and B while both are online — verify the most-recent update wins and no mastery regression occurs. (5) Regression: confirm existing paying-user flow still works — sign-in, child list loads, subscription gate functions, no data loss on upgrade. Also run `npm run build` to confirm it passes clean for Amplify deploy. Report pass/fail per scenario plus any console errors; if any fail, file the exact repro steps so Engineer can patch before commit.

---

