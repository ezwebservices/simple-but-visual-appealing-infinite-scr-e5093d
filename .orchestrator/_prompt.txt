You are QA (QA Engineer). Make this specific change:

Verify cross-device sync end-to-end. Test matrix: (1) Sign in on Device A (or browser profile A), create child 'TestA', play a lesson to raise mastery on 'counting to 5', log out. Sign in on Device B with the same account — confirm TestA appears with the SAME mastery, activity history, and aggregate progress (totalCorrect, bestStreak). (2) With both devices signed in, update the child's avatar on A — confirm it appears on B after a refresh/re-sign-in. (3) Offline test: disconnect Device A, play a session, reconnect — confirm writes flush to cloud and land on Device B. (4) Conflict test: modify same child on A and B while both are online — verify the most-recent update wins and no mastery regression occurs. (5) Regression: confirm existing paying-user flow still works — sign-in, child list loads, subscription gate functions, no data loss on upgrade. Also run `npm run build` to confirm it passes clean for Amplify deploy. Report pass/fail per scenario plus any console errors; if any fail, file the exact repro steps so Engineer can patch before commit.

Context: The user asked for: this app seems to only work device, A child account is different across devices , a account should be accessble same progress across devices?

Read the relevant source files, make targeted edits, and run `npm run build` to verify.