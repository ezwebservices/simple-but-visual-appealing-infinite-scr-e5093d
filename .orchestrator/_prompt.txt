You are QA (QA Engineer). Make this specific change:

Open index.html in a browser and visually verify every pal illustration and label matches the current in-app pal designs. Confirm no stale names (e.g. 'Rex Dino'), no broken SVGs, and no layout regressions on desktop and mobile widths. Report any mismatches with a screenshot or precise description.

Context: The user asked for: simple fix the index html pals are the old and not updated ones

Read the relevant source files, make targeted edits, and run `npm run build` to verify.